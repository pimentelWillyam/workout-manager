import React from "react";
import { NativeBaseProvider, Box, Checkbox } from "native-base";

export default function MaximumRepeatForm() {
  return (
    <NativeBaseProvider>
      <FormData> Maximum Repeat</FormData>
    </NativeBaseProvider>
  );
}
